diff --git a/dist/api/package.js b/dist/api/package.js
index 8fb0bea8296b4426389441e2ade33d976262f865..6166eadb0c4383362d8431b74981cb345eb4a245 100644
--- a/dist/api/package.js
+++ b/dist/api/package.js
@@ -163,7 +163,9 @@ const listrPackage = (childTrace, { dir: providedDir = process.cwd(), interactiv
                         done();
                     }),
                     hidePromiseFromPromisify(async (buildPath, electronVersion, pPlatform, pArch, done) => {
-                        const bins = await (0, fast_glob_1.default)(node_path_1.default.join(buildPath, '**/.bin/**/*'));
+                        // ATM patch (ATM-T-0488): fast-glob treats "\" as an escape, so on Windows
+                        // path.join(buildPath, pattern) loses its absolute base and walks process.cwd().
+                        const bins = await (0, fast_glob_1.default)('**/.bin/**/*', { cwd: buildPath, absolute: true });
                         for (const bin of bins) {
                             await fs_extra_1.default.remove(bin);
                         }
