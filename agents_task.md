# Agent 施工进度（最近 15 条）

- 2026-08-08：ATM-T-0038～0043 实现已完成至 70%（progress seq 429～434）：抽屉左边缘 `>` 收起、未注册项目自动创建、可独立验收 WorkItem 拆分硬规则、Agent 按项目与身份聚合但保留历史数量、工程统计默认折叠且展开才请求、活动项目仅两行全名并在纯英文过长时提示使用中文；定向 Vitest 7/7、Playwright 4/4 全绿，三张截图人工复核正常，待全量验证、打包与 GitHub CI。
- 2026-08-08：ATM-T-0038～0043 本地完整验收通过：format/lint/typecheck、38 文件 65 tests、9 E2E、production build 全绿；`pnpm make` 生成新版 EXE 与 Setup，packaged smoke 14/14、真实 window smoke 通过，左缘 `>` 命中区 44×86、关闭到托盘正常；Guide 与完整 docs 已同步正式 `%LOCALAPPDATA%\AyanamiTaskManager` 且 SHA-256 与源码一致，六项 checklist 均 DONE/v1，待提交、推送和 GitHub CI。
- 2026-08-08：功能提交 `d4b819a` 已推送，GitHub Actions `31278360978` 在干净 Windows runner 的 install/format/lint/typecheck/65 tests/build 全绿；最终 FACT `ATM-R-039` seq 447，ATM-T-0038～0043 progress seq 448～453 后全部 verify/complete 为 DONE/100%（v12，T-0043 v13）。新版裸 EXE SHA-256 `F6373445...CCD490`，Setup SHA-256 `D9F6B6FC...4F4261`；待提交本条审计记录并关闭主 Session。
- 2026-08-08：ATM Session `01KZHP4PGMNK7BG8440ZYG5AJP` 下 ATM-T-0045～0047 已依次 DONE。`DISCOVERED_FROM` 以正式 v4 migration 完成：v3→v4 保留 BLOCKS，支持已有 key/同批前向 ref，ready/cycle 非阻塞，外键级联、REST/MCP、双向详情、层级标记、搜索、事件与 JSON/.aytproj 快照均覆盖；5 项关系定向测试、REST 8/8 和 6 包 typecheck 全绿，最终任务 DONE/100%/v10。下一叶子为 Planning Playbooks。
- 2026-08-08：ATM Session `01KZHR9FVY17S6H90S1C5VDY0P` 领取 ATM-T-0053 并完成 Git Context REST 验收与文档补齐：真实 daemon REST 测试覆盖 begin 从 cwd 确定性采集、dirty 手动刷新及持久化；api 9/9、daemon typecheck、ESLint、Prettier 与 diff-check 全绿。
- 2026-08-08：子 Session `01KZHRBR8NHVB7JDWVQR3FMWYF` 已领取并开始 ATM-T-0054，准备在 `apps/desktop/e2e/desktop.spec.ts` 验收 Agent Git context 主信息/展开详情/冲突警告/刷新请求与项目页 Agent、任务执行 Session，保留暗色截图并仅运行定向 E2E 与 lint/diff-check。
- 2026-08-08：ATM-T-0049 Git/Worktree/Session 可视化父叶子已闭环至 DONE/100%/v25：确定性只读 Git helper、v5 Session 持久化、meaningful progress/verify/complete/end/手动刷新边界、同 branch/worktree 警告、Agent/项目/任务三处显示均完成；15 项定向场景、3 项迁移、REST 9/9 与 Agent E2E 3/3 通过，FACT 与最终 progress seq 529 已写。E2E 另发现 1280px Agent 表首列裁切，已并入 ATM-T-0051 验收。
- 2026-08-08：时间线主任务 ATM-T-0050 已按可交付结果拆成 T0055 后端语义投影与 T0056 UI 可读呈现；T0055 由 Luna 独立领取。主线程 T0056 已到 60%/seq543：新增统一 timeline-events 归一化（新旧 schema 兼容）及 3 项红绿测试，全局时间线、总览最近变化、项目时间线均展示项目/任务、标题、详情、actor、序列与时间，并补窄屏长文本样式，待后端落地后做真实 E2E。
- 2026-08-09：ATM-T-0050～0052 全部闭环：时间线改投影真实 outbox 事件，项目/global delta 统一可读 title/detail/project/task/actor，历史 `project.summary.updated` 仅过滤展示；Agent 页按项目和身份聚合且保留完整 Session 历史，1280px 裁切消失；侧栏总览/项目常驻、其余工作区单点折叠、设置固定底部。storage 4 项、UI 3 项测试及时间线/Agent/侧栏 Playwright 4 项全绿，三张暗色/亮色原图人工复核；FACT ATM-R-043～046，T0050 DONE/v23、T0051 DONE/v10、T0052 DONE/v8。
- 2026-08-09：Agent-Native 增强候选版完整 `pnpm release --resume` 已全量通过：format/lint/typecheck、46 文件 95 tests、13 E2E、benchmark、build/Forge、packaged/portable/installed 各 14/14、distribution 23/23 与 assemble-release 全绿；正式安装后 Guide、25 docs、3 skills 与源码清单一致，ATM 数据及 T0044 均保留。发布中发现的 Squirrel `.dead` + 快捷方式遗留已拆为 ATM-T-0058，并以严格三条件、精确清理边界和用户数据保留证据闭环 DONE/100%/v8；待冻结提交后在干净 HEAD 复跑最终发布并推送 GitHub CI。
- 2026-08-09：Agent-Native 增强及发布韧性已在功能 HEAD `3f1a0ff` 闭环：Windows Git 8.3/长路径统一 canonicalization，E2E readiness 容忍短暂拒连，数据库原子 rename 仅对 Windows EACCES/EBUSY/EPERM 有限退避；本地干净 HEAD release 的 format/lint/typecheck、46 文件 95 tests、13 E2E、benchmark、build/Forge、packaged/portable/installed 各 14/14、distribution 23/23、真实窗口 smoke 全绿，Guide/25 docs/3 skills 与安装清单一致且历史保留。GitHub Actions `31284846867` 在 Windows runner 14 步全绿；ATM-T-0058～0061 均 DONE，Setup 169.04 MiB/SHA-256 `AA13BC79...DBF5B6`、portable 174.00 MiB/`84E76E2A...C89E1A`。本条为最终审计记录，待 docs-only 提交后按同一门禁复验最终 HEAD。
- 2026-08-11：ATM-T-0064/0065 已闭环：Claude Code 通过真实 `claude` CLI 管理 user-scope stdio MCP，设置页明确区分 Codex/Claude Desktop/Claude Code，并正确展示共享规则技能与 CLI 不可用禁用态；产品升至 1.0.1。发布中发现并修复 stale `out/make` 令旧 1.0.0 ZIP 被误验收/重命名的缺陷，distribution 与 assemble 现按 `package.json.version` 精确取产物。完整 release 从头通过 46 文件 97 tests、13 E2E、benchmark、build/Forge、packaged/portable/installed 各 14/14、distribution 28 项；Setup SHA-256 `EB052EBD...BFD15B1`、portable `AF88F4FB...CE562BA`。正式 EXE/daemon 均为 1.0.1，7 项目保留；打包 MCP 实测 `atm_brief(ATM-T-0062)` 默认 1114 字成功、非法 include 明确拒绝。待提交后在干净 HEAD 复跑最终 release、推送并确认 GitHub CI。
- 2026-08-19：ATM-T-0068 已闭环：检查项证据死结修复（dc83d9b）——`assertCompletionGate` 的证据查询漏掉 `SKIPPED`，令标注「需要证据」的勾选项既勾不动也跳不过，UI 侧补上内联挂证据、跳过与恢复；只读扫描 9 个项目库量化影响为 188 个无证据的证据必填项阻塞 5 个项目的 54 个未完成任务。产品升至 1.0.2（296eb46）。发布中发现并修复 `distribution-smoke` 调用裸 `tar.exe` 的缺陷：PATH 上 Git 的 GNU tar 1.35 会把 `R:\...` 当成 `host:path` 去解析主机名，已钉到 System32 的 bsdtar 并补静态守卫、阳性对照与扫描面非空断言（2db4b89）。完整 `pnpm release` 十项全绿：105 tests、13 E2E、benchmark、build/Forge、packaged/portable/installed 各 14/14、distribution 28/28 与 assemble-release；Setup SHA-256 `BBEA7E56…996FC9BB`、portable `7F850338…D967A28FA`。安装版实测证据闸门：两项需证据勾选项均 TODO 时 complete 报 `COMPLETION_GATE_FAILED: checklist incomplete`，A 项挂证据后可置 DONE、B 项可置 SKIPPED，再 complete 返回 ok。另记：Agent 的 shell 通道对 `%LOCALAPPDATA%` 的创建不落真盘（删除却是真的），distribution-smoke 必须由用户在本机终端执行，本轮据此误判过一次杀软拦截，详见 ATM-R-063。发现的同族缺陷 ATM-T-0069（WAITING_USER/BLOCKED 声明可回 IN_PROGRESS 但无操作可执行）已单独立项。
- 2026-08-20：ATM-T-0069 闭环并随 1.0.3 发出（修复 `d59c4b8`、升版 `d057445`）。根因是状态机有两份真话：界面照 `WORK_ITEM_TRANSITIONS` 画出口，仓储层另写一份来源状态白名单，导致四个已发布按钮必然 `INVALID_TRANSITION`——BLOCKED / WAITING_USER / WAITING_AGENT 的「重新打开」与 VERIFYING 的「退回」。转移表改为唯一事实源，八个改状态的操作全部只 assert 它：reopen 把停住的任务拉回 IN_PROGRESS（仅 CANCELLED 退回 BACKLOG）并清掉已失效的 `blocked_reason`/`waiting_for`；claim/start 删掉硬编码白名单、start 对进行中任务幂等；release 补上缺失的 assert，DONE/CANCELLED 不再被悄悄拉回 READY。守卫 `work-item-transitions.test.ts` 双向校验（声明必可达 + 不得走出未声明），三条根因逐条写回去验红、两处同时还原时可达性用例报 4 条不可达，基线前后均绿。1.0.3 十阶段全绿：test 50 文件 111 用例、13 E2E、benchmark 7/7 阈值、packaged/portable/installed 各 14/14、distribution 23/23 与 assemble-release；Setup SHA-256 `6BC6D746…8CEFC`、portable `9C45A5BA…E50814`，四件产物哈希装机后重算一致。安装版实测 `/api/v1/system/status` 报 `1.0.3`、`ok`、9 个项目库 quick_check 通过（数据完整穿过卸载重装），四个出口沿按钮自己的 `POST /ui/work-items/patch` 逐个走通且等待/阻塞原因被清空。发布中发现：`distribution-smoke` 只按镜像名 tasklist 判断「有没有应用在跑」，而 MCP stdio 桥用同一个 exe 名并占着安装目录句柄——只要还有 Claude 会话连着 ATM，验收就永远过不去且报错看不出是谁在占，已记入清单待补诊断。另：1.0.2 清单 32 个勾里有 6 条从来没有对应用例（v1/v2 迁移、MCP schema 8KB、Ctrl+K/N、归档→垃圾箱链路、托盘与二次启动唤起、5 分钟 LRU 策略值），本轮按清单自身规矩改回未验证并列为非阻塞剩余项。同事未提交的 session begin 幂等改动全程 stash 保管，发布后恢复（ATM-T-0072）。
- 2026-08-21：ATM-T-0073 闭环并随 1.0.4 发出（修复 `a51ba2e`、升版 `55fdc60`）。根因家族是「闸门判你，而你够不着那个闸门」，三条通道一并扫掉：检查项（完成闸门第一道就是 checklist，而 11 个 MCP 工具没有一个能碰它，Guide 与 agent-integration 零处提及 REST 退路——新增 `atm_checklist`）；blockers 记录（带非空 `blocker` 的 progress 会 INSERT 一条 ACTIVE，而全仓库只有那一处 INSERT 和闸门一处 SELECT，没有 UPDATE/DELETE，任务被写过一次就永远完不成——改为 reopen/start 时一并解除）；404 伪装（Fastify 对「路径存在但方法没注册」也回 404，有人用 POST 试 checklist 拿到 404 便判定端点不存在，转去猜路径名——改为 405 + allow 头）。守卫四组逐条验红；其中 cancel 侧解除验红仍绿（blockers 表无任何展示读取，不可观测），已删除而非留一行无守卫覆盖的代码。1.0.4 十阶段全绿：54 文件 120 用例、13 E2E、benchmark 7/7 阈值、packaged/portable/installed 各 14/14、distribution 28/28；Setup SHA-256 `53ADE8B5…A205C5`、portable `56D8C030…A290BEC2`，四件产物装机后重算一致。装机版实测 `tools/list` 12 个工具含 `atm_checklist`、schema 8059/8192，blocker 出路整条走通。另修正一条把三轮发布都拖成手工的错误认知：「%LOCALAPPDATA% 安装类验收 Agent 做不了」只对 Bash 通道成立，PowerShell 通道写入真盘——本轮发布由 Agent 全自动完成，用户未介入任何一步（ATM-R-067 取代 ATM-R-063）。过程中还犯并纠正了一次：用 `git apply --unidiff-zero` 只暂存自己的 hunk 时关掉了上下文校验，剔除同事 hunk 后行号偏移把代码切进别的语句，只 grep 内容、未在暂存快照上跑 lint 所以没暴露；两个提交未推送，已回退重做并在「工作树等于提交内容」的状态下复验。同族第三处（MCP 建不了 Objective，新项目里一个任务都建不出来）另立 ATM-T-0074。
